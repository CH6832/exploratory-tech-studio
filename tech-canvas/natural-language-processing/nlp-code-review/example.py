# This function needs to be optimized for speed.
def calculate_sum(nums):
    total = 0
    for num in nums:
        total += num
    return total

# This loop should be more efficient.
def find_max(nums):
    max_num = nums[0]
    for num in nums:
        if num > max_num:
            max_num = num
    return max_num

# This code could be improved for better performance.
def process_data(data):
    # Process the data
    pass
