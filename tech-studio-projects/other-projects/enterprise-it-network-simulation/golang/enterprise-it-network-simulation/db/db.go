package db

import "fmt"

// SaveSimulationResult saves the simulation result to the database
func SaveSimulationResult(result string) {
	// Placeholder function for saving results to DB
	fmt.Printf("Saving simulation result: %s\n", result)
}
