#include <iostream>

/**
 * @brief Simulates the Open Shortest Path First (OSPF) protocol.
 */
class OSPF {
public:
    void route() {
        std::cout << "Routing using OSPF..." << std::endl;
    }
};
