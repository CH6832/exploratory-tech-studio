spring.application.name=config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/config-repo # Your Git repository URL
          # Optional: Add credentials if needed
          # username: your-username
          # password: your-password
      # Or use a local directory
      # native:
      #   search-locations: file://${user.home}/config-repo/
