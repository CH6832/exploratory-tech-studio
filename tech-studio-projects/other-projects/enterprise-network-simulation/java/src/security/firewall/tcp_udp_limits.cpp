#include <iostream>

/**
 * @brief Limits TCP/UDP access for the firewall.
 */
class TcpUdpLimits {
public:
    void limitAccess() {
        std::cout << "Limiting TCP/UDP access." << std::endl;
    }
};
