pluginManagement {
    repositories {
        gradlePluginPortal()  // Add this to ensure plugin resolution
        mavenCentral()        // Optionally add Maven Central if needed
    }
}

rootProject.name = "kotlin"
