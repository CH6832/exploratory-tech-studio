package com.fintech.algotrading.logging;

//Interface for text-based logging that also requires resource cleanup.
public interface ITextLogger extends ILogger {
 // Optionally, you can define additional abstract methods specific to text logging.
 // Example: void setLogFile(String filePath);
}

