Sequence Diagram

Purpose: To show how objects interact in a sequence of events.

    Actors:
        User
        Frontend (React)
        Backend (Flask)
        Database (MySQL)

    Scenario:
        User submits data through Frontend.
        Frontend sends data to Backend.
        Backend processes data and stores it in Database.
        Backend sends results back to Frontend.
        Frontend updates the User interface.