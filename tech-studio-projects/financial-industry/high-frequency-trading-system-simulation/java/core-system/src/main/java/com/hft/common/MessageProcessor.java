package com.hft.common;

/**
 * Processes incoming messages, tuned to minimize parsing latency.
 */
public class MessageProcessor {

    /**
     * Processes the message payload and routes it to the correct handler.
     */
    public void processMessage(String payload) {
        // Parse and handle message
    }
}
